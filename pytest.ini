; Keeps pytest anchored to this project. Without a config file here, pytest
; walks up the directory tree and picks up whatever it finds first, which on a
; developer machine is rarely this repo.
[pytest]
testpaths = tests
addopts = -q
